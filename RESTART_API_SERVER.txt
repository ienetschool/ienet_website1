API SERVER RESTART INSTRUCTIONS

From your screenshots, I can see:
✅ Node.js is configured with mysql-production-server.cjs
✅ mysql2 dependency is installed  
❌ API still returning 502 errors

NEXT STEPS:

1. TEST DATABASE CONNECTION:
   ssh root@5.181.218.15
   cd /var/www/vhosts/vivaindia.com/ienet.online/
   node check-database-status.js

2. SETUP DATABASE (if connection works):
   node setup-mysql-database.js

3. RESTART NODE.JS APPLICATION:
   - Go back to Plesk Node.js panel
   - Click "Restart App" button
   - Wait for status to show "Running"

4. TEST API:
   curl https://www.ienet.online/api/health
   curl https://www.ienet.online/api/service-categories

TROUBLESHOOTING:
- If database connection fails, check MySQL service status
- If Node.js won't start, check application logs in Plesk
- If 502 errors persist, verify Nginx proxy configuration

The setup script will show you exactly what's happening with your database connection and fix any missing tables or data automatically.