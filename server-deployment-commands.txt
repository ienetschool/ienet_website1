# Final deployment verification commands:

cd /var/www/vhosts/vivaindia.com/ienet.online

# Check PM2 status
pm2 status

# Ensure PM2 is saved and will restart on reboot
pm2 save
pm2 startup

# Check if the application is responding properly
curl -I http://localhost:3000
curl -I http://ienet.online

# Check Nginx status
systemctl status nginx

# Check application logs
pm2 logs ienet --lines 20

# Final test - the website should now be working
echo "Testing ienet.online - should now show your IeNet application:"
curl -s http://ienet.online | head -20