EXACT SOLUTION FOR YOUR PLESK CONFIGURATION:

STEP 1: CREATE FOLDER STRUCTURE
In Plesk File Manager, create this exact structure in /ienet.online/:

/ienet.online/
  ├── dist/
  │   └── index.js
  ├── package.json
  └── public/
      ├── index.html
      ├── assets/
      │   ├── index-BOus7yXH.js
      │   ├── index-5acz5IyP.css
      └── IE vector logo-01_1755535165852.png

STEP 2: COPY FILES

File 1: /ienet.online/dist/index.js
```javascript
import express from 'express';
import { fileURLToPath } from 'url';
import { dirname, join } from 'path';

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const app = express();
const PORT = process.env.PORT || 3000;

app.use(express.static(join(__dirname, '..', 'public')));

app.get('*', (req, res) => {
  res.sendFile(join(__dirname, '..', 'public', 'index.html'));
});

app.listen(PORT, () => {
  console.log(`IeNet React App running on port ${PORT}`);
});
```

File 2: /ienet.online/package.json
```json
{
  "name": "ienet-production",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

File 3: Copy COPY_THIS_TO_PLESK.html as /ienet.online/public/index.html

STEP 3: COPY ASSETS
Download plesk-dist-deployment.tar.gz and extract:
- Copy public/assets/ folder to /ienet.online/public/assets/
- Copy logo file to /ienet.online/public/

STEP 4: PLESK SETTINGS
Keep current settings:
- Application Startup File: dist/index.js
- NPM install
- Restart App

RESULT: React website shows on ienet.online